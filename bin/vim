#!/usr/bin/env bash

type kak 2>/dev/null >/dev/null && {
    echo >&2 "Kak is available! Use it :-p"
    exit 1
}

component="$(dirname $0)"
PATH=":$PATH:"
PATH="${PATH/:$component:/:}"
PATH="${PATH%:}"; PATH="${PATH#:}"
vim "$@"
