#!/usr/bin/env bash

. ~/.local/lib/utils.bash

#bash_enable_exit_on_error
check_binary "dmenu"
check_binary "xbacklight"

function set_backlight() {
    xbacklight -steps 1 -time 0 -set "$1"%
}
dmenu_command "Screen Brightness" print_percent_intervals set_backlight
